{
  "name": "untitled2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create:html:report": "marge --reportDir testreport cypress/results/*.json",
    "clean:reports": "rm -rf cypress/results/",
    "smoke": "cypress run --config-file smoke.config.ts --headed",


    "merge:reports": "npm run clean:reports; npm run cy:run; mochawesome-merge cypress/reports/*.json > cypress/reports/full_report.json",
    "report:copyScreenshots": "cp -r cypress/screenshots cypress/reports/screenshots",
    "generate:report": "npm run merge:reports; --reportDir testreport cypress/results/*.json; npm run report:copyScreenshots"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/cypress-cucumber-preprocessor": "^4.0.1",
    "cucumber": "^6.0.7",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "cypress-real-events": "^1.7.6",
    "mochawesome-report-generator": "^6.2.0",
    "save-dev": "^0.0.1-security"
  },
  "devDependencies": {
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^10.4.0",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1",
    "typescript": "^4.9.4"
  },
  "cypress-cucumber-preprocessor": {
    "step_definitions": "cypress/integration/step_definitions/**/"
  }
}
